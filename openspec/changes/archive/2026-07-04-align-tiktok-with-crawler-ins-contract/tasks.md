## 1. 接口与契约适配

- [x] 1.1 改造 `POST /fetch` 入参解析: 支持 `starId/accountId` 二选一,保持 `202/400` 语义与现有响应结构
- [x] 1.2 为 `server` 层补充 `starId` 兼容路径测试(仅 starId、双字段、缺失字段)
- [x] 1.3 增加账号标识归一化工具,统一在服务边界将 `starId` 映射为内部账号标识

## 2. 账号列表来源切换为 instar 协议

- [x] 2.1 改造账号来源客户端,仅解析 `{code:0,data:{list:[{starId}]}}`
- [x] 2.2 对非法结构、`code!=0`、空 `starId` 增加失败测试并确保本轮对账不落库
- [x] 2.3 更新配置与文档说明,明确账号列表接口契约与鉴权用法

## 3. 抓取完成回调适配

- [x] 3.1 新增 `instar` 完成回调客户端(账号级),发送 `{starId, token:"instar", status:1|0}`
- [x] 3.2 在账号抓取成功/失败结束处接入回调调用,不影响抓取与去重主流程
- [x] 3.3 为成功与失败回调路径补充测试,验证 payload 与触发时机

## 4. 集成验证与迁移收尾

- [x] 4.1 运行并修复类型检查与测试(`bunx tsc --noEmit`、`bun test`)
- [x] 4.2 追加迁移说明: `instar` 如何改调 `/fetch` 与账号列表接口,以及 `crawler-ins` 下线步骤
- [x] 4.3 回归校验 `/status` 与周期调度行为未被本次适配破坏
